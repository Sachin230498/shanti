let arr = [5, 4, 6, 8, 9, 10];

// console.log(arr[3]);

// No. of elements ==> 6

// Last Index of the Array ==> 5

// This means the last index will always be 1 less than the actual no. of elements

// N elements are there in an array

// What is the last index? ==> N-1



let trains = ["Rajdhani", "Shatabdi", "Duronto", "Gareeb Rath"];

// Total No. of trains?
console.log("Total trains are:", trains.length);

// Which is the last train?
// This means i want to access the train present at last index
// let last_index=trains.length-1;

console.log("Last train is:", trains[trains.length - 1]);